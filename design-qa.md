# 后台运营统计：第一版设计验收

final result: passed

日期：2026-09-21。用户选择第一版设计，另要求 URL 使用 `#` 保留页面位置。

## 对照依据与状态

- Source visual truth：`/Users/sssrank/.codex/visualizations/2026/09/21/website-analytics-review/design-1.png`。
- Implementation：`http://127.0.0.1:4173/admin#analytics/users`；密集数据验收使用仅监听 loopback 的隔离服务 4174。
- 最终桌面截图：`/Users/sssrank/.codex/visualizations/2026/09/21/website-analytics-implementation/13-users-final.png`。
- 实际站点截图：`/Users/sssrank/.codex/visualizations/2026/09/21/website-analytics-implementation/16-real-service-final.png`。
- 最终窄屏截图：`/Users/sssrank/.codex/visualizations/2026/09/21/website-analytics-implementation/14-mobile-final.png`。
- 桌面 CSS viewport 为 1440 × 1024，截图为 1440 × 1024，输出密度 1 px/CSS px。移动 viewport/截图均为 390 × 844。
- 原图为 1487 × 1058，无浏览器边框；没有可读取的 CSS 尺寸。按其完整画面的相同比例比较构图（宽度换算约 0.9684），未将约 3% 的生成图尺寸差异作为字号或间距缺陷，不主张逐像素复刻。
- 对照状态：浅色主题、已登录、用户明细、开发环境、2026-08-23 至 2026-09-21、默认字段。原图为 128 人示意，隔离数据为 12 个虚构用户，实际站点当前只有 1 人，人数及业务数值不作为视觉差异。
- 源图与最终桌面实现已在同一次图像输入中一起打开比较。完整图可直接读清标题、导航、分组表头、数字和行操作；另已查看分页菜单、窄屏顶部和表格区域，不需要裁剪放大才能识别细节。

## 比较历史与已修复问题

| 轮次 | 发现 | 修复与后续证据 |
| --- | --- | --- |
| 1，初始桌面 `03-fixture-desktop.png` | P2：顶部间隔和行高偏大，首屏表格密度低于第一版；搜索框受到全局输入框样式影响，出现重复边框 | 收紧标题、筛选、指标区和表格间距；调整行 padding、滚动区域高度和搜索输入样式优先级。`13-users-final.png` 与原图共同对照：首屏可见约 8 行、分页，主要区域比例与第一版接近 |
| 2，窄屏 `11-mobile-users.png` | P2：数据时间的“北京时间”末字单独换行 | 时间与时区按完整片段排列；`14-mobile-final.png` 已复查，无孤立末字，页面 scrollWidth 与 viewport 均为 390 |
| 3，采集状态 `09-health.png` | P2：历史拒收空态曾建议调整日期，与历史累计范围矛盾 | 改为“服务尚未记录被拒收的事件。”；`15-health-final.png` 已重新打开核对，与下方口径一致 |

上述截图均位于 `/Users/sssrank/.codex/visualizations/2026/09/21/website-analytics-implementation/`。最终无待处理的 P0/P1/P2 发现。

## 五项视觉检查

- **字体与层级**：沿用项目 Inter / Segoe UI / PingFang SC / Microsoft YaHei 回退。28px 主标题、19px 分区标题、14px 表格正文，数字使用等宽排列；姓名与工号分行，日期紧凑显示且可查看完整时间。层级与原图一致，未复制生成图中的文字栅格瑕疵。
- **布局与间距**：224px 持续侧栏，内容两侧 28px，筛选、指标条、工具栏、表格按原图顺序排列。固定表头和身份列；桌面页内滚动表格，窄屏滚动限制在表格容器中。分页和全局导航均可访问。
- **颜色与 tokens**：沿用 `--accent-primary: #4f46e5`、现有正文/次级文字/边框 tokens；浅紫当前导航、行 hover/focus 和操作按钮形成一致强调。无新增花哨阴影或装饰背景。
- **图片与资源**：直接复用现有 `/brand.svg` 和项目 `Icon.vue` 标准图标；Logo 清晰、比例正常。该设计不需要照片或插画，未以整张设计图替代可操作页面。
- **文案与数据**：使用真实 API 返回值，不在生产页面展示“设计示例数据”或虚构管理员姓名。保留匿名数据、用量缺失、Token 推理包含关系和历史拒收的说明。版本是现有 API 支持的文本筛选，未伪造一个没有版本数据源的下拉框。

## 交互与回归

- 用户搜索按姓名/工号作用于完整结果；跨分页升降序结果正确，分页前进行排序。活跃排名保留固定规则。
- 列设置应用、再次打开持久化、恢复默认、Escape 取消与焦点恢复通过。
- 分页、每页条数菜单、Escape 关闭通过；第 2 页和每页 25 条刷新后保持。
- 用户详情跳转及返回恢复搜索、排序、分页；浏览器前进/后退恢复对应 Hash。
- 环境切换到生产并刷新仍保留环境，空数据清楚呈现；不把没有采集记录解释成完整覆盖。
- 文档选择 `#guides/models` 刷新后保持；未保存文档通过侧栏跳转和浏览器后退都出现应用内确认。取消保留草稿和 URL，确认放弃后跳转。临时草稿未保存。
- 文档编辑、目录管理、发布包、图片库、知识库及 8 个统计子页均完成导航检查；公开官网的原顶栏仍显示，返回后台和刷新正常。
- 窄屏导航抽屉打开/关闭、Escape 和焦点恢复通过；窄屏表格滚动区域与分页可见。减少动态效果的规则覆盖后台按钮和统计区域，未添加动画。
- 浏览器实际站点与隔离服务的 error/warn 控制台记录均为空。
- HTTP 检查 context、overview、trends、users、rankings、features、conversations、skills、operations、health 均为 200、有效 JSON、`Cache-Control: no-store`；API 集成测试覆盖无效查询、鉴权与来源限制。此项是独立 HTTP 检查，不冒称浏览器网络面板录制。
- `pnpm check`：类型检查通过，74/74 测试通过。`pnpm build`：通过。

## 可接受差异与限制

- 原图 8 条/页是静态示意，实现使用 10/25/50/100 条；表格内部滚动保持首屏结构。根据真实总数生成分页，未照搬示意的 16 页。
- 复用项目字体、主题 tokens 和标准图标，图标外形、颜色不追求生成图逐像素一致。
- 移动端是原图未给出的响应式补充；使用折叠导航和横向表格。
- 在 macOS 内置浏览器完成验收，未声称完成 Windows 浏览器或安装包验收。本次不改变客户端更新协议。

## 完成检查

- [x] 第一版主要结构和表格层级完成。
- [x] Hash 路由、筛选、分页与登录恢复完成。
- [x] 视觉问题修复后重新捕获并比较。
- [x] 桌面、窄屏、键盘与未保存编辑保护检查完成。
- [x] 类型检查、测试和生产构建通过。
- [x] 隔离测试服务停止；临时数据库与配置清理。
