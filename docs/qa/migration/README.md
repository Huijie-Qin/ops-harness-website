# 独立仓库迁移验收 · 2026-09-14

源码基线：产品仓库提交 `1403193331b3fa1d4e638d153d7d03ea43dc91f9`。原 `src/website` 的 65 个文件全部迁入本仓库，只有 README、package.json、服务端 TypeScript 继承路径、两份配置的默认位置和发布 CLI 使用示例需要适配；页面、业务服务、Markdown、实拍图片和原有测试保持原字节。

官网维护手册、相关 ADR、历史验收与截图一并迁入。原仓库已移除官网 workspace、构建脚本及源码，保留协议源包和 Desktop 更新配置。官网通过本仓库的版本制品消费同一份协议，不依赖相邻 checkout。原源码与配置备份保留在原仓库忽略目录 `.runtime/website-migration-20260914`；原内容、发布运行目录也保留。迁移时运行目录无已保存文件，11 个 Markdown 基线均完整保留。

## 自动检查

- 官网全新依赖安装后，`pnpm install --frozen-lockfile --offline` 通过；未链接原仓库的 node_modules 或源码。
- `pnpm check`：Vue / 服务端类型检查通过，28 项测试通过。覆盖原有管理员鉴权、CSRF、并发、文档历史、目录、图片、上传与发布、下载/更新契约，以及新增的独立默认配置、含空格外部配置路径和协议制品完整性。
- `pnpm build`：生产前端、服务端、11 篇 Markdown 与 Vditor 本地资源构建成功。
- 原仓库冻结锁文件安装通过；共享协议 4 项、Desktop 更新与独立官网定位 11 项、workspace 构建编排 11 项测试通过。Desktop 类型检查通过，集成定位器实际加载新仓库的生产 handler 与 publisher 成功。
- 独立生产服务的 17 个页面、API 和资源请求均返回 200；三张 JPEG 的响应字节与原图相同，管理员 CSP 包含固定图标脚本哈希。
- 两仓库 workflow YAML 解析通过；新仓库未包含运行数据、安装包、node_modules、生成资源或符号链接。当前维护文档链接与差异空白检查通过。

## 浏览器验收

Chrome，1470 px。生产预览使用隔离的 4187 端口和运行目录。

- 管理员登录成功，Vditor 正文、工具栏及本地附属资源正常显示。
- 帮助文档、发布包、目录配置、图片库四个入口可切换。目录显示 3 个分组和 11 篇文档；发布列表与图片库显示真实空状态。
- 阅读页章节导航正常，工具章节的真实 JPEG 解码为 1470 × 746，图文显示正常，页面无横向溢出。
- 首页显示原有动效、实拍切换与下载区；暂停和播放均可切换。Console 无 error / warn。
- 常驻开发服务由新仓库启动于 4173；使用此前指定的管理员密码登录成功。凭据仅在进程环境中，不写入文件。

[生产管理页面截图](admin-directory.jpg)

验收结束已停止 4187 并清理隔离目录，保留 4173。没有修改真实文档或发布测试安装包到常驻目录。本次未运行全产品仓库检查、Windows Electron / NSIS 目标机烟测或云端 CI；跨仓库 Windows 烟测已迁至要求固定官网提交 SHA 的独立手动 workflow，正式发布前仍须执行。未提交或推送 Git。
