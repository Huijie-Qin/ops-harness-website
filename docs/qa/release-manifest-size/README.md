# 更新描述文件大小兼容验收（2026-09-15）

环境：macOS arm64，Node.js 24.16.0、pnpm 11.19.0，真实 Google Chrome（Playwright 驱动）；使用临时配置、loopback 服务和隔离发布/内容目录。密码只在进程环境中注入。未连接或启动 DSH，未触碰在线发布内容。

- 官网 `pnpm check`：类型检查通过，49 项测试通过。
- 官网 `pnpm build`：通过。
- 产品仓库新增的描述文件校验与原打包入口测试：26 项全部通过。
- 产品 `pnpm check:desktop`：类型检查通过，183 项测试中 182 项通过。既有 `apps/desktop/test/packaging.test.mjs:151` 的目录选择器断言失败：测试仍要求平台条件表达式，当前 `src/main.mjs:636` 已固定为 `'1'`；这两个文件本次均未修改。

浏览器验收：

1. 导入用户 0.1.3 YAML 的缩进修正版，自动识别版本、平台和哈希，无 `size` 时显示“上传后读取大小”。见 [导入截图](legacy-manifest.jpg)。
2. 将根级字段错误缩进到文件条目后，服务端返回 400，提示同级重复字段及正确缩进，焦点定位到错误。见 [错误截图](duplicate-key.jpg)。
3. 对临时模拟安装包调用产品仓库 `verifyUpdateManifest`，复核哈希并补齐大小；该实际输出成功导入官网，识别的字节数与源文件一致。
4. 使用没有大小的同一描述文件创建隔离草稿，同长度但字节不同的上传返回 409；重传正确内容成功，页面显示实测 53 B、校验通过。见 [桌面截图](verified-desktop.jpg) 和 [390 px 窄屏截图](verified-mobile.jpg)。
5. 减少动态效果模式下无横向溢出；发布确认框 Escape 关闭并恢复触发按钮焦点，Enter 可重新打开。确认发布后公开 feed 保留原 SHA-512，并包含实测大小。
6. 切换文档编辑、图片库、目录与文档管理、发布包及公开更新说明页正常。Console 无新增异常，网络仅有上述预期 400/409，服务端无异常。

临时服务、浏览器、脚本和测试存储已清理。测试字节仅用来验收文件传输，从未执行。未在 Windows 原生构建、安装或升级，不以本次 macOS 测试代替目标平台验收。
